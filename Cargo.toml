[package]
name = "agent-shim"
version = "0.2.0"
edition = "2024"

[dependencies]
tiny_http = "0.12"
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
